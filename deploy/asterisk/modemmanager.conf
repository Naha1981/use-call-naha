; Replace the two identifiers with values returned by mmcli.

[modem1]
type = modem
identifier = CHANGE_MODEM_IDENTIFIER
; Uncomment for Quectel modems that need USB voice audio enabled:
; init_commands = AT+QPCMV=1,2

[sim1]
type = sim
identifier = CHANGE_SIM_ICCID
context = from-mobile
exten = s
